import { ConfigEnv, PluginOption } from "vite";

//#region src/index.d.ts
interface GenEnvTypeOptions {
  mode: ConfigEnv['mode'];
  envDir: string;
  envPrefix: string | string[];
  filePath: string;
  valueInType: boolean;
}
type EnvTypedOptions = Partial<GenEnvTypeOptions>;
declare const envTyped: ({
  mode,
  envDir,
  envPrefix,
  filePath,
  valueInType
}?: EnvTypedOptions) => PluginOption;
//#endregion
export { EnvTypedOptions, envTyped as default };